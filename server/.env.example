# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/woworld_db?schema=public"

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRATION=1d

# Redis Configuration (for future use)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TTL=3600 # Time to live in seconds

# API Configuration
API_PREFIX=api
API_VERSION=v1

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100
